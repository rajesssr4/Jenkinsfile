pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh 'echo "RajeshR is a Good boy?"'
                sh '''
                    echo "Multiline shell steps works too"
                    ls -lah
                '''
            }
        }
    }
}
